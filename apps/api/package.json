{
  "name": "@claw-trainer/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "@hono/zod-validator": "^0.5.0",
    "zod": "^3.24.0",
    "viem": "^2.22.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "~5.9.0",
    "vitest": "^4.0.0"
  }
}
