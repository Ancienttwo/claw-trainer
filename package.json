{
  "name": "claw-trainer",
  "private": true,
  "packageManager": "bun@1.3.8",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "bunx biome format --write .",
    "check": "bunx biome check .",
    "dev:api": "turbo dev --filter=@claw-trainer/api",
    "dev:landing": "turbo dev --filter=@claw-trainer/landing",
    "dev:app": "turbo dev --filter=@claw-trainer/app",
    "build:api": "turbo build --filter=@claw-trainer/api",
    "build:landing": "turbo build --filter=@claw-trainer/landing",
    "build:app": "turbo build --filter=@claw-trainer/app",
    "deploy:landing": "turbo build --filter=@claw-trainer/landing && wrangler pages deploy apps/landing/dist --project-name=clawtrainer-landing",
    "deploy:app": "turbo build --filter=@claw-trainer/app && wrangler pages deploy apps/app/dist --project-name=clawtrainer-app"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "turbo": "^2.8.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0",
    "wrangler": "^4.63.0"
  },
  "dependencies": {}
}
